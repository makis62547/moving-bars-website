---
import Header from "./Header.astro";
---

<section id="hero" class="hero">
  <div class="hero-bg parallax-bg" data-parallax="bg" data-parallax-speed="0.25" role="presentation"></div>
  <div class="hero-overlay" aria-hidden="true"></div>

  <Header />

  <div class="hero-center">
    <ul class="hero-usp">
      <li><span class="hero-check">✔</span><span>Faire Preise</span></li>
      <li><span class="hero-check">✔</span><span>Langjährige Erfahrung</span></li>
      <li><span class="hero-check">✔</span><span>Beste Cocktails & Longdrinks</span></li>
      <li><span class="hero-check">✔</span><span>Für Privat & Firmen – Events</span></li>
    </ul>

    <div class="hero-arrow">⌄</div>
  </div>
</section>

<section class="intro" aria-labelledby="intro-title">
  <div class="intro-shell">
    <div class="intro-panel">
      <div class="intro-text">
        <h1 id="intro-title" class="intro-title">
          <span class="intro-title-accent">MOBILE COCKTAILBAR</span> MIETEN IN BERLIN
        </h1>
        <p class="intro-copy">
          Wir lieben Cocktails, perfekten Getränke-Service und wir lieben Veranstaltungen. Mieten Sie unsere mobile Cocktailbar
          in Berlin oder üBARall. Von der Vorbereitung und Planung bis zur Durchführung sind wir Ihr begeisterter Partner, wenn
          es darum geht ein besonderes Erlebnis für Ihre Gäste entstehen zu lassen. Die mobile Cocktailbar, der perfekte Drink,
          der schnelle Service, der Barkeeper, der bei der 2. Bestellung weiß, was man trinkt oder einfach das Lächeln, mit dem
          das Getränk serviert wurde – wir wollen begeistern!
        </p>
      </div>

      <div class="intro-divider" aria-hidden="true"></div>

      <div class="intro-icon" aria-hidden="true"></div>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: 100vh;
    width: 100%;
    overflow: hidden;
    background: #000;
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    background-image: url("/images/hero.jpg");
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
  }

  .hero-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(180deg, rgba(0, 0, 0, 0.6) 0%, rgba(0, 0, 0, 0.55) 100%);
    z-index: 5;
  }

  :global(header) {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    height: var(--header-h);
    overflow: visible;
    z-index: 40;
  }

  /* Center layer: keep overall centering, but nudge the USP block up/right */
  .hero-center {
    position: absolute;
    inset: 0;
    display: flex;
    flex-direction: column;
    align-items: center;      /* stays visually in the middle */
    justify-content: center;
    text-align: left;
    color: #fff;
    padding: 140px 1.5rem 0;
    width: 100%;
    z-index: 20;
    transform: translateY(-18px);
  }

  .hero-usp {
    display: inline-grid;
    gap: 1.75rem;
    font-size: clamp(1.2rem, 2.1vw, 1.8rem);
    font-weight: 200; /* thinner */
    font-family: "Inter", system-ui, sans-serif; /* lighter feel than Montserrat-heavy headings */
    text-transform: uppercase;
    line-height: 1.6;
    list-style: none;
    padding: 0;
    margin: 0;
    width: min(820px, 92vw);

    /* NUDGE: a bit up and a bit right (tweak values as needed) */
    transform: translate(22px, -62px);
  }

  .hero-usp li {
    display: flex;
    align-items: center;
    gap: 0.6rem;
    opacity: 0;
    transform: translateY(10px);
    animation: uspIn 700ms ease forwards;
  }

  .hero-usp li:nth-child(1) { animation-delay: 0ms; }
  .hero-usp li:nth-child(2) { animation-delay: 250ms; }
  .hero-usp li:nth-child(3) { animation-delay: 500ms; }
  .hero-usp li:nth-child(4) { animation-delay: 750ms; }

  .hero-check {
    color: #d38b45;
    margin-right: 0.5rem;
  }

  @keyframes uspIn {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-usp li {
      animation: none;
      opacity: 1;
      transform: none;
    }
  }

  .hero-arrow {
    margin-top: 3rem;
    font-size: clamp(2.5rem, 4vw, 3.75rem);
    color: rgba(255, 255, 255, 0.9);
    animation: bounce 1.5s infinite;
  }

  @keyframes bounce {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(10px); }
  }

  :global(header nav) {
    display: flex;
    gap: 56px;
    white-space: nowrap;
  }

  .intro {
    background: #000;
    padding: 44px 0 96px;
  }

  .intro-shell {
    max-width: 1240px;
    margin: 0 auto;
    padding: 0 3rem;
    display: flex;
    justify-content: flex-start;
  }

  .intro-panel {
    display: grid;
    grid-template-columns: minmax(0, 0.62fr) 1px minmax(0, 0.38fr);
    align-items: flex-start;
    background: var(--background);
    padding: clamp(1.5rem, 3vw, 2.5rem) clamp(1.5rem, 4vw, 3rem);
    color: #f7f7f7;
    font-family: "Inter", system-ui, sans-serif;
    letter-spacing: normal;
    width: 100%;
  }

  .intro-text {
    display: grid;
    gap: 1rem;
    padding-right: clamp(1.25rem, 3vw, 2rem);
    margin-top: -10px;
  }

  .intro-title {
    margin: 0;
    font-size: clamp(2.5rem, 4.8vw, 3.75rem);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    font-weight: 300;
  }

  .intro-title-accent {
    color: #d38b45;
    display: inline-block;
    margin-right: 0.35ch;
  }

  .intro-copy {
    margin: 0;
    font-size: 1rem;
    line-height: 1.7;
    font-weight: 300;
    color: rgba(255, 255, 255, 0.92);
  }

  .intro-divider {
    width: 1px;
    background: rgba(211, 139, 69, 0.7);
    margin: clamp(0.4rem, 1.5vw, 0.85rem) clamp(1.4rem, 3vw, 2.4rem);
    align-self: stretch;
    border-radius: 0;
  }

  .intro-icon {
  min-height: 100%;
  margin-top: 6px;
  display: flex;
  align-items: flex-start;
  justify-content: center;
}

.intro-icon::before {
  content: "";
  display: block;
  width: clamp(56px, 6vw, 76px);
  height: clamp(56px, 6vw, 76px);
  background: radial-gradient(circle at 30% 30%, rgba(211, 139, 69, 0.85), rgba(211, 139, 69, 0.6));
  mask-image: radial-gradient(circle, #000 65%, transparent 70%);
  border: 1px solid rgba(211, 139, 69, 0.4);
}


  @media (max-width: 900px) {
    .intro-panel {
      grid-template-columns: 1fr;
      row-gap: 1.5rem;
    }

    .intro-divider {
      height: 1px;
      width: 100%;
      margin: clamp(1rem, 3vw, 1.5rem) 0;
      background: rgba(211, 139, 69, 0.7);
    }

    .intro-text {
      padding-right: 0;
    }

    .intro-icon {
      min-height: 0;
    }

    .intro-shell {
      padding: 0 1.5rem;
    }
  }
</style>
