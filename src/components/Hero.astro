---
// src/components/Hero.astro
import Header from "./Header.astro";
import CocktailIcon from "./icons/CocktailIcon.astro";
---

<section id="hero" class="hero">
  <!-- Parallax Background -->
  <div class="hero-bg" aria-hidden="true"></div>
  <div class="hero-overlay" aria-hidden="true"></div>

  <Header />

  <div class="hero-center">
    <div class="hero-shell">
      <ul class="hero-usp">
        <li><span class="hero-check">✔</span><span>Faire Preise</span></li>
        <li><span class="hero-check">✔</span><span>Langjährige Erfahrung</span></li>
        <li><span class="hero-check">✔</span><span>Beste Cocktails & Longdrinks</span></li>
        <li><span class="hero-check">✔</span><span>Für Privat & Firmen – Events</span></li>
      </ul>

      <a
        class="hero-arrow"
        href="#hero-h1"
        aria-label="Zum Abschnitt: Mobile Cocktailbar mieten in Berlin"
      >
        ⌄
      </a>
    </div>
  </div>
</section>

<section id="hero-h1" class="intro" aria-labelledby="intro-title">
  <div class="intro-shell site-container">
    <div class="intro-panel">
      <div class="intro-text">
        <h1 id="intro-title" class="intro-title">
          <span class="intro-title-accent">MOBILE COCKTAILBAR</span><br />
          MIETEN IN BERLIN
        </h1>

        <p class="intro-copy">
          Wir lieben Cocktails, perfekten Getränke-Service und wir lieben Veranstaltungen…
        </p>
      </div>

      <div class="intro-divider" aria-hidden="true"></div>

      <div class="intro-icon" aria-hidden="true">
        <CocktailIcon />
      </div>
    </div>
  </div>
</section>

<style>
/* ================= HERO ================= */

.hero {
  position: relative;
  min-height: 100vh;
  overflow: hidden;
  background: #000;
}

/* 🔥 DAS ist der Parallax-Layer */
.hero-bg {
  position: absolute;
  inset: -20vh 0 0 0;        /* Überhöhe */
  height: 140vh;
  background-image: url("/images/hero.jpg");
  background-size: cover;
  background-position: center;
  will-change: transform;
  transform: translateY(0);
}

/* Scroll-driven Animation (modern browsers) */
@supports (animation-timeline: scroll()) {
  .hero-bg {
    animation: hero-parallax linear forwards;
    animation-timeline: scroll();
    animation-range: entry 0% exit 100%;
  }

  @keyframes hero-parallax {
    from { transform: translateY(0); }
    to   { transform: translateY(18vh); }
  }
}

.hero-overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(
    180deg,
    rgba(0,0,0,0.6),
    rgba(0,0,0,0.55)
  );
  z-index: 5;
}

.hero-center {
  position: relative;
  z-index: 10;
  min-height: 100vh;
  display: flex;
  align-items: center;
}

.hero-shell {
  max-width: var(--content-max);
  margin: 0 auto;
  padding: 140px var(--content-pad);
}

/* ================= REST BLEIBT WIE GEHABT ================= */
.hero-usp { /* unverändert */ }
.hero-arrow { /* unverändert */ }
.intro { background: #000; }
</style>
