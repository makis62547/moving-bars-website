---
const highlights = [
  { label: "Events", value: "Corporate · Privat · Festivals" },
  { label: "Drinks", value: "Signature Cocktails & Zero-Proof" },
  { label: "Team", value: "Barkeeper · Logistik · Planung" },
];
---

<section class="border-b border-white/10 bg-black" id="hero">
  <div class="mx-auto max-w-6xl px-6 py-14">
    <div class="grid gap-10 md:grid-cols-2">
      <div class="flex flex-col gap-6">
        <p class="text-xs uppercase tracking-[0.35em] text-white/60">Mobile Cocktailbar Berlin</p>
        <h1 class="text-4xl font-bold leading-tight md:text-5xl">
          Bewegende Bars, kompromisslose Drinks und ein Team, das Events präzise orchestriert.
        </h1>
        <p class="text-sm leading-relaxed text-white/75">
          Maßgeschneiderte Cocktailbars, erfahrene Barkeeper und logistische Präzision für Auftritte in Berlin und deutschlandweit.
          Wir liefern klare Linien, schnelle Abläufe und Getränke mit Charakter.
        </p>
        <div class="flex flex-wrap gap-3">
          <a
            class="bg-[#D38B45] px-5 py-3 text-sm font-semibold uppercase tracking-[0.2em] text-black transition-colors hover:bg-[#bb783d]"
            href="#contact"
          >
            Jetzt anfragen
          </a>
          <a
            class="border border-white/20 px-5 py-3 text-sm font-semibold uppercase tracking-[0.2em] text-white transition-colors hover:border-[#D38B45] hover:text-[#D38B45]"
            href="#services"
          >
            Leistungen
          </a>
        </div>
        <div class="grid grid-cols-1 gap-4 border-t border-white/10 pt-5 sm:grid-cols-3">
          {highlights.map((item) => (
            <div class="flex flex-col gap-1" aria-label={item.label}>
              <p class="text-[11px] uppercase tracking-[0.35em] text-white/50">{item.label}</p>
              <span class="text-sm font-medium text-white">{item.value}</span>
            </div>
          ))}
        </div>
      </div>

      <div class="flex flex-col gap-3">
        <div class="flex-1 border border-white/10 bg-black">
          <img
            alt="Mobile Cocktailbar"
            class="h-full w-full object-cover"
            src="/images/placeholder-hero.jpg"
          />
        </div>
        <div class="grid grid-cols-2 gap-3">
          <div class="border border-white/10 bg-black p-4">
            <p class="text-[11px] uppercase tracking-[0.35em] text-white/50">Response</p>
            <p class="text-sm font-medium">48h Angebot & Planung</p>
          </div>
          <div class="border border-white/10 bg-black p-4">
            <p class="text-[11px] uppercase tracking-[0.35em] text-white/50">Kapazität</p>
            <p class="text-sm font-medium">20 bis 2000 Gäste</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
