---
const sliderImages = [
  "/images/placeholder-1.jpg",
  "/images/placeholder-2.jpg",
  "/images/placeholder-3.jpg",
  "/images/placeholder-4.jpg",
];
---

<section class="under-hero-tiles" aria-label="Ergänzende Informationen">
  <div class="tiles-shell">
    <!-- TILE 1 (Vorlage): Icon links | orangener Strich | H2 + Text -->
    <article class="tile tile--intro">
      <div class="intro">
        <div class="intro__icon" aria-hidden="true">
          <!-- Platzhalter-Icon (3 Personen) -->
          <svg viewBox="0 0 128 128" width="140" height="140" role="img" aria-label="">
            <g fill="currentColor">
              <circle cx="64" cy="42" r="18" />
              <path d="M34 108c0-18 14-32 30-32s30 14 30 32H34z" />
              <circle cx="30" cy="50" r="14" opacity="0.95" />
              <path d="M8 108c0-14 11-25 24-25s24 11 24 25H8z" opacity="0.95" />
              <circle cx="98" cy="50" r="14" opacity="0.95" />
              <path d="M72 108c0-14 11-25 24-25s24 11 24 25H72z" opacity="0.95" />
            </g>
          </svg>
        </div>

        <div class="intro__sep" aria-hidden="true"></div>

        <div class="intro__copy">
          <h2>PRIVATE FEIER – MESSEPARTY – GROSSVERANSTALTUNG</h2>
          <p>
            Kurzer Blindtext als Platzhalter. Hier steht ein kompakter Absatz, der den Nutzen der Leistung in ein bis
            zwei Sätzen beschreibt.
          </p>
        </div>
      </div>
    </article>

    <!-- TILE 2: Text links / Slider rechts -->
    <article class="tile">
      <div class="tile-copy">
        <p class="tile-label">Slider rechts</p>
        <p class="tile-text">
          Links Text, rechts ein horizontaler Slider mit quadratischen Bildern. Scrollbar oder per Buttons navigierbar.
        </p>
      </div>
      <div class="tile-visual">
        <div class="slider" data-slider>
          <button class="slider-btn prev" type="button" aria-label="Vorheriges Bild">‹</button>
          <div class="slider-track" tabindex="0">
            {sliderImages.map((src, index) => (
              <div class="slide" aria-label={`Platzhalterbild ${index + 1}`}>
                <img alt="Platzhalter" src={src} loading="lazy" />
              </div>
            ))}
          </div>
          <button class="slider-btn next" type="button" aria-label="Nächstes Bild">›</button>
        </div>
      </div>
    </article>

    <!-- TILE 3: Slider links / Text rechts -->
    <article class="tile">
      <div class="tile-visual">
        <div class="slider" data-slider>
          <button class="slider-btn prev" type="button" aria-label="Vorheriges Bild">‹</button>
          <div class="slider-track" tabindex="0">
            {sliderImages.map((src, index) => (
              <div class="slide" aria-label={`Platzhalterbild ${index + 1}`}>
                <img alt="Platzhalter" src={src} loading="lazy" />
              </div>
            ))}
          </div>
          <button class="slider-btn next" type="button" aria-label="Nächstes Bild">›</button>
        </div>
      </div>
      <div class="tile-copy">
        <p class="tile-label">Slider links</p>
        <p class="tile-text">
          Links der quadratische Slider, rechts kurzer Blindtext. Auch hier nur Platzhalter – Inhalte folgen später.
        </p>
      </div>
    </article>

    <!-- TILE 4: Icon links / Text rechts (simple placeholder) -->
    <article class="tile">
      <div class="tile-visual icon" aria-hidden="true"></div>
      <div class="tile-copy">
        <p class="tile-label">Placeholder Headline</p>
        <p class="tile-text">
          Noch ein kurzer Blindtext mit Icon als Akzent. Alle Angaben sind temporär und dienen nur dem Layout-Test.
        </p>
      </div>
    </article>
  </div>
</section>

<script>
  const sliders = document.querySelectorAll('[data-slider]');

  sliders.forEach((slider) => {
    const track = slider.querySelector('.slider-track');
    const prev = slider.querySelector('.slider-btn.prev');
    const next = slider.querySelector('.slider-btn.next');

    if (!track || !prev || !next) return;

    const scrollAmount = () => track.clientWidth * 0.9;

    prev.addEventListener('click', () => {
      track.scrollBy({ left: -scrollAmount(), behavior: 'smooth' });
    });

    next.addEventListener('click', () => {
      track.scrollBy({ left: scrollAmount(), behavior: 'smooth' });
    });
  });
</script>

<style>
  .under-hero-tiles {
    background: #000;
    color: #f7f7f7;
    padding: clamp(2.5rem, 6vw, 4.5rem) var(--content-pad) clamp(3rem, 8vw, 5rem);
  }

  .tiles-shell {
    max-width: var(--content-max);
    margin: 0 auto;
    display: grid;
    gap: clamp(1.5rem, 3vw, 2.25rem);
  }

  /* Base tile */
  .tile {
    display: grid;
    grid-template-columns: 1fr;
    gap: clamp(1rem, 3vw, 1.5rem);
    padding: clamp(1.25rem, 3vw, 1.75rem);
    border: 1px solid rgba(255, 255, 255, 0.08);
    background: rgba(255, 255, 255, 0.02);
  }

  .tile-visual {
    display: grid;
    place-items: center;
  }

  .tile-visual.icon {
    width: clamp(72px, 12vw, 96px);
    height: clamp(72px, 12vw, 96px);
    border-radius: 50%;
    background: radial-gradient(circle at 35% 35%, rgba(211, 139, 69, 0.9), rgba(211, 139, 69, 0.45));
    border: 1px solid rgba(211, 139, 69, 0.5);
    box-shadow: 0 0 0 8px rgba(211, 139, 69, 0.08);
  }

  .tile-copy {
    display: grid;
    gap: 0.35rem;
    font-family: "Inter", system-ui, sans-serif;
  }

  .tile-label {
    font-size: clamp(1.05rem, 2.2vw, 1.35rem);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    margin: 0;
    color: #fff;
  }

  .tile-text {
    margin: 0;
    color: rgba(247, 247, 247, 0.8);
    line-height: 1.7;
    font-weight: 300;
  }

  /* Slider */
  .slider {
    position: relative;
    width: 100%;
  }

  .slider-track {
    display: grid;
    grid-auto-flow: column;
    grid-auto-columns: minmax(160px, 1fr);
    gap: 0.85rem;
    overflow-x: auto;
    padding: 0.25rem 0.5rem;
    scroll-snap-type: x mandatory;
    scroll-padding: 0.5rem;
    scrollbar-width: thin;
  }

  .slider-track::-webkit-scrollbar {
    height: 6px;
  }

  .slider-track::-webkit-scrollbar-thumb {
    background: rgba(255, 255, 255, 0.2);
    border-radius: 999px;
  }

  .slide {
    scroll-snap-align: start;
    border: 1px solid rgba(255, 255, 255, 0.12);
    background: rgba(255, 255, 255, 0.04);
    border-radius: 10px;
    overflow: hidden;
    position: relative;
  }

  .slide img {
    width: 100%;
    height: 100%;
    aspect-ratio: 1 / 1;
    object-fit: cover;
    display: block;
  }

  .slider-btn {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    background: rgba(0, 0, 0, 0.65);
    color: #fff;
    border: 1px solid rgba(255, 255, 255, 0.2);
    width: 36px;
    height: 36px;
    border-radius: 50%;
    display: grid;
    place-items: center;
    cursor: pointer;
    transition: background 150ms ease, border-color 150ms ease, opacity 150ms ease;
  }

  .slider-btn:hover,
  .slider-btn:focus-visible {
    background: rgba(211, 139, 69, 0.95);
    border-color: rgba(211, 139, 69, 0.8);
    outline: none;
  }

  .slider-btn.prev { left: 0; }
  .slider-btn.next { right: 0; }

  @media (max-width: 720px) {
    .slider-btn {
      display: none;
    }
  }

  @media (min-width: 900px) {
    .tile {
      grid-template-columns: minmax(0, 0.95fr) minmax(0, 1.05fr);
      align-items: center;
    }

    .tile:nth-of-type(2n) {
      grid-template-columns: minmax(0, 1.05fr) minmax(0, 0.95fr);
    }
  }

  /* --- Tile 1 exakt wie Vorlage: Icon | orangener Strich | H2 + Text --- */
  .tile--intro {
    border: none;
    background: transparent;
    padding: 0;
  }

  .tile--intro .intro {
    display: grid;
    grid-template-columns: auto 1px minmax(0, 1fr);
    align-items: center;
    gap: clamp(1rem, 3vw, 2.5rem);
  }

  .tile--intro .intro__icon {
    color: rgba(211, 139, 69, 0.95);
    width: clamp(140px, 16vw, 190px);
    display: grid;
    place-items: center;
  }

  .tile--intro .intro__sep {
    align-self: stretch;
    background: rgba(211, 139, 69, 0.75);
  }

  .tile--intro .intro__copy h2 {
    margin: 0;
    color: rgba(211, 139, 69, 0.95);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    font-weight: 500;
    font-size: clamp(1.35rem, 2.6vw, 2.5rem);
    line-height: 1.12;
  }

  .tile--intro .intro__copy p {
    margin: 0.85rem 0 0;
    color: rgba(247, 247, 247, 0.82);
    line-height: 1.75;
    font-weight: 300;
    max-width: 80ch;
  }

  @media (max-width: 720px) {
    .tile--intro .intro {
      grid-template-columns: 1fr;
      gap: 1rem;
    }
    .tile--intro .intro__sep {
      width: 100%;
      height: 1px;
    }
    .tile--intro .intro__icon {
      width: auto;
      justify-items: start;
    }
  }
</style>
