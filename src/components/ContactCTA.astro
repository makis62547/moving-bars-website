<section id="kontakt" class="contact-cta max-w-6xl mx-auto grid lg:grid-cols-2 gap-10 items-center">
  <div class="space-y-4">
    <p class="text-orange-300 uppercase tracking-[0.2em] text-sm">Kontakt</p>
    <h2 class="text-3xl md:text-4xl">Lassen Sie uns Ihr Event planen</h2>
    <p class="text-white/70">
      Erzählen Sie uns von Ihrem Anlass, der Gästeanzahl und Ihren Vorstellungen. Wir melden uns mit einem maßgeschneiderten
      Konzept binnen 24 Stunden.
    </p>
    <ul class="space-y-3 text-white/80">
      <li class="flex items-center gap-3"><span class="text-orange-300">✔</span> Persönlicher Ansprechpartner</li>
      <li class="flex items-center gap-3"><span class="text-orange-300">✔</span> Transparente, faire Kalkulation</li>
      <li class="flex items-center gap-3"><span class="text-orange-300">✔</span> Aufbau, Service & Abbau inklusive</li>
    </ul>
    <div class="flex flex-wrap gap-3">
      <a href="mailto:hallo@movingbars.de" class="rounded-full border border-frame px-5 py-3 font-semibold border-accent-hover">
        hallo@movingbars.de
      </a>
      <a href="tel:+493011223344" class="rounded-full border border-frame px-5 py-3 font-semibold border-accent-hover">
        +49 (0)30 112 233 44
      </a>
    </div>
  </div>

  <div class="rounded-3xl border border-frame bg-white/5 p-6 shadow-xl space-y-4">
    <h3 class="text-xl">Schnellanfrage</h3>
    <form class="space-y-4">
      <div class="grid md:grid-cols-2 gap-4">
        <input type="text" placeholder="Name" class="input" />
        <input type="email" placeholder="E-Mail" class="input" />
      </div>
      <div class="grid md:grid-cols-2 gap-4">
        <input type="tel" placeholder="Telefon" class="input" />
        <input type="text" placeholder="Eventdatum" class="input" />
      </div>
      <textarea placeholder="Kurzbeschreibung des Events" class="input min-h-[120px]"></textarea>
      <button
        type="button"
        class="w-full rounded-full bg-orange-500 px-6 py-3 text-black font-semibold uppercase tracking-wide hover:bg-orange-400 transition"
      >
        Anfrage senden
      </button>
      <p class="text-white/60">Wir melden uns innerhalb eines Werktags mit Rückfragen oder einem konkreten Angebot.</p>
    </form>
  </div>
</section>

<style>
  section.contact-cta {
    padding: var(--contact-pad-block) var(--section-pad-inline);
  }

  .border-frame {
    border-color: var(--border);
  }

  .border-accent-hover:hover {
    border-color: var(--accent-light);
  }
</style>
